Here is the English translation of your prompt, written from the perspective of a professional client/hiring manager. The tone is set to be authentic, direct, and technically grounded.
Project Title: GreenEnergy Data Pipeline & Dashboard
Client Overview:
"Hey there! We are a startup specializing in renewable energy services, and we need an internal monitoring tool for our power plants. Currently, our data comes from on-site sensors and is stored in CSV files and occasionally SQL databases. We need this entire system to run within a Docker environment so our engineers can deploy it locally (offline) with a single command. Crucially, the final image sizes must be as small as possible because our on-site internet speeds are quite limited."
Why I’m Hiring You:
I was really impressed by your environment setup (WSL2) and your choice of Alpine and Slim distributions to save space. It shows me that you truly understand Performance and Optimization, which is exactly what we need for this project.
Technical Requirements:
 * Backend: A Flask application served via Gunicorn.
 * Database: PostgreSQL for user data storage and Redis for high-speed caching.
 * Data Processing: Utilize Pandas and NumPy to process energy data uploaded via CSV.
 * Frontend: A clean, functional interface using Jinja2 templates with Nginx acting as a Reverse Proxy.
 * Monitoring: I need to see the health of our containers through Prometheus and Grafana.
Project Assets (To be provided):
 * data_sample.csv: Contains energy readings (Volts, Amps, Temp).
 * requirements.txt: My preferred library versions.
 * Company Logo (Image asset).
Your Required Workflow:
Since you’ll be working offline and pushing the final results, here is the roadmap:
1. Docker Multi-stage Build
 * Because we are obsessed with footprint, you must use python:slim or python:alpine.
 * Implement a Multi-stage build to ensure build-time dependencies (like compilers) are excluded from the final production image.
2. Docker Compose Orchestration
 * Link all 5 services: Flask, Postgres, Redis, Nginx, and Grafana.
 * Configure Volumes to ensure data persistence when containers are restarted.
3. CI/CD Pipeline (GitHub Actions)
 * Create a .github/workflows/main.yml file.
 * Tasks: Upon code push, the action must verify the Docker images build correctly and run a Linting process (using tools like Hadolint).
 * Perform a security vulnerability scan using Trivy.
Expected GitHub Project Structure:
 * app/: Flask source code and Pandas data processing logic.
 * nginx/: Reverse Proxy configuration files.
 * docker-compose.yml: The orchestrator for the entire stack.
 * Dockerfile: Professional, optimized Multi-stage file.
 * README.md: Clear documentation on how to launch the project via docker-compose up -d.
The Goal:
This isn't a "Hello World" project. This is a Full Stack Pipeline. You are solving real-world constraints: optimization (DevOps), security (Trivy), data persistence (Postgres/Redis), and reliability (Monitoring). I’m looking for a production-ready solution that proves you can handle industrial-grade requirements.
Would you like me to generate the optimized Multi-stage Dockerfile or the Docker Compose structure for this specific project?
